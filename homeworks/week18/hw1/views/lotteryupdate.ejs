<!DOCTYPE html>

<html>
<head>
  <meta charset="utf-8">

  <title>餐廳官網</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://necolas.github.io/normalize.css/8.0.1/normalize.css"/>
<style>
  <%- include('../public/css/style.css')%>
</style>
</head>

<body>
  <%- include('template/navbar')%>
  <section class="game__banner">
  <img src="../images/games-bn.jpg">
    <div class="back">
      <div class="info">
       <% if(username) { %>
        <div class="prize__form_container">
        <% if(errorMessage && errorMessage.length > 0) { %>
          <div class="error">
            <%= errorMessage%>
          </div>
        <% }%>
          <form method="POST" action="/update_prize/<%= lottery.id%>">
            <div class="prize">
              獎項名稱
              <input name="prizename" value="<%= lottery.prizename%>">
            </div>
            <div class="prize">
              圖片網址
              <input name="imgurl" value="<%= lottery.imgurl%>">
            </div>
            <div class="prize">
              獎項機率
              <input name="chances" value="<%= lottery.chances%>">
            </div>
            <input class="prize__submit-btn" type="submit" value="更新">
          </form>
          <% } %>
        </div>
      </div>
    </div>
  </section>
  <%- include('template/footer')%>
</body>
</html>
