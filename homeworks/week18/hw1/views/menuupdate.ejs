<!DOCTYPE html>

<html>
<head>
  <meta charset="utf-8">

  <title>餐廳官網</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://necolas.github.io/normalize.css/8.0.1/normalize.css"/>
<style>
  <%- include('../public/css/style.css')%>
</style>
</head>

<body>
  <%- include('template/adminNavbar')%>
  <section class="game__banner">
  <img src="../images/games-bn.jpg">
    <div class="back">
      <div class="info">
       <% if(username) { %>
        <div class="prize__form_container">
        <% if(errorMessage && errorMessage.length > 0) { %>
          <div class="error">
            <%= errorMessage%>
          </div>
        <% }%>
          <form method="POST" action="/update_product/<%= product.id%>">
            <div class="prize">
              菜單名稱
              <input name="productsname" value="<%= product.productsname%>">
            </div>
            <div class="prize">
              圖片網址
              <input name="imgurl" value="<%= product.imgurl%>">
            </div>
            <div class="prize">
              價格
              <input name="prices" value="<%= product.prices%>">
            </div>
            <input class="prize__submit-btn" type="submit" value="更新">
          </form>
          <% } %>
        </div>
      </div>
    </div>
  </section>
  <%- include('template/footer')%>
</body>
</html>
